language: node_js

env:
  - DATABASE_URL || 'postgresql://codex:coder123@localhost:5432/greetings_tests';

node_js:  
  - "8.9.4"

before_script:
  - psql -c 'create database  greetings_test;' -U postgres
  - psql -U postgres -d greetings_test -a -f tables.sql

addons:
    apt:
    packages:
    - postgresql-server-dev-9.2